[*.cs]

# CA1812: Program.CommandlineOptions is an internal class that is apparently never instantiated. If so, remove the code from the assembly. If this class is intended to contain only static members, make it static (Shared in Visual Basic).
# => False positive because of CommandlineParser.
dotnet_diagnostic.CA1812.severity = none
